export interface IDevice {
  docId: string;
  SKUID: string;
  expirationDate: string;
  format: string;
  imageURL: string;
  lastestVersion: string;
  macAddress: string;
  memory: string;
  name: string;
  note: string;
  operatingLocation: string;
  password: string;
  status: string;
  unitsUsed: string;
  userName: string;
}
