export interface IRecordPlay {
    id: string;
    recordsId: string;
    playsCount: string;
    date: string;
}

export interface RecordPlays {
    id: string;
    recordsId: string;
    playsCount: string;
    date: string;
  }